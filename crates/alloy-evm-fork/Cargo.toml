[package]
name = "alloy-evm"
description = "EVM abstraction for Alloy (PRAPH fork with op-revm fork)"
version = "0.24.1"
edition = "2021"
rust-version = "1.88"
license = "MIT OR Apache-2.0"
homepage = "https://github.com/alloy-rs/evm"
repository = "https://github.com/alloy-rs/evm"

[lints]
workspace = true

[dependencies]
alloy-consensus = { workspace = true, features = ["k256"] }
alloy-primitives.workspace = true
alloy-sol-types.workspace = true
alloy-eips.workspace = true
alloy-hardforks.workspace = true
alloy-op-hardforks = { workspace = true, optional = true }
alloy-rpc-types-eth = { workspace = true, optional = true }
alloy-rpc-types-engine = { workspace = true, optional = true }

revm.workspace = true
# PRAPH: Use local op-revm fork instead of upstream
op-revm = { path = "../op-revm-fork", optional = true }
op-alloy-consensus = { workspace = true, optional = true }
op-alloy-rpc-types = { workspace = true, optional = true }

auto_impl.workspace = true
derive_more.workspace = true
thiserror.workspace = true

[dev-dependencies]
alloy-primitives = { workspace = true, features = ["serde"] }
serde_json.workspace = true
test-case.workspace = true

[features]
default = ["std"]
secp256k1 = [
    "std",
    "alloy-consensus/secp256k1",
]
std = [
	"alloy-primitives/std",
	"revm/std",
	"alloy-consensus/std",
	"alloy-eips/std",
	"alloy-sol-types/std",
	"derive_more/std",
	"op-revm?/std",
	"thiserror/std",
	"op-alloy-consensus?/std",
	"alloy-rpc-types-eth?/std",
	"alloy-rpc-types-engine?/std"
]
op = ["op-revm", "op-alloy-consensus", "alloy-op-hardforks"]
overrides = ["dep:alloy-rpc-types-eth"]
call-util = ["overrides"]
engine = ["dep:alloy-rpc-types-engine"]
asm-keccak = ["alloy-primitives/asm-keccak", "revm/asm-keccak"]
rpc = ["dep:alloy-rpc-types-eth", "dep:op-alloy-rpc-types"]
